# Moved

`mage-os/module-extension-directory` is now developed in the
[mage-os.directory](https://github.com/rhoerr/mage-os.directory) repository, which
holds both the directory service (`service/`) and this Magento admin module (`src/`).
This repository's history was merged there intact.

Do not open pull requests here.
